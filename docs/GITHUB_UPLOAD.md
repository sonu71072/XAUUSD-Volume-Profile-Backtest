# GitHub Update

From the project root:

```powershell
git status
git add .
git status
git commit -m "Professionalize research documentation and project structure"
git push
```

Before committing, verify that raw CSV files and generated results remain ignored.
